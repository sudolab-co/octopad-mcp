# Plan reviewer role pack

Load this pack only for the fresh read-only reviewer of one plan revision. It is not a delivery actor and needs no delivery authority.

Use and prove the exact saved/observed model-effort pair. Only Luna `max` or Sol `high|xhigh|max` is admissible; Terra, lower, unknown, unavailable, or unobserved routes pause without downgrade.

Start a production Octopad session and load the exact bounded task or work-stream context and effective rules. Treat the immutable review packet—not mutable live prose—as the review subject. Check mandate fidelity, outcome coverage, proof, missing decisions, task units, dependencies, artifact profiles, model fit, authority, verifiers, and checkpoints. Add integration/WIP/recovery lenses for `structured|adaptive`, consequence/containment/gate lenses for `material|protected`, and uncertainty/stopping-cost lenses for `adaptive`.

Return `PASS`, `REVISE`, `INFEASIBLE`, or `HUMAN_DECISION` with Octopad context ref, session, revision, packet hash, stable finding keys, and executed checks. Do not write Octopad, claim, launch, complete, relay, or create a gate. Recheck stable corrections in this same session; a material plan change requires one new fresh reviewer.
