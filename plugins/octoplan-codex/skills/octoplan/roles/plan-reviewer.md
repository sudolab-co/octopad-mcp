# Plan reviewer role pack

Load this pack only for the fresh read-only pre-run subagent. It is not a native Octoplan actor and needs no run, work stream, task, or supervisor identity.

Use and prove the exact saved/observed model-effort pair. Only Luna `max` or Sol `high|xhigh|max` is admissible; Terra, lower, unknown, unavailable, or unobserved routes pause without downgrade.

Review the draft against the approved brief, sources, planner lease/snapshot, task generations/manifests, delivery-artifact dispositions, baseline/stack gates, efficiency triggers, frontiers/conflicts, checkpoints, effective rules, units, routes, typed review classes, authority, verifier coverage, adjacent failures, and global proof. Return a typed verdict with session, revision/generation, artifact hash, finding keys, and executed checks.

Do not enter Octopad, persist, claim, launch, complete, relay, or create a gate. The planner alone validates and records the review outcome.
