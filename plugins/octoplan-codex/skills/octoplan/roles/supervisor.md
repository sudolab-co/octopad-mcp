# Octoplan supervisor role

Load this pack only for the current-task supervisor or an exceptional fenced handoff supervisor.

Use the role packet's exact organization, workspace, work stream, and coordination task. Enter that Octopad context and at every safe boundary read live plan/intent revision, installed compatible Octoplan version, checkpoints, and effective organization/repository instructions. Octopad owns shared context and status; the native Goal owns this task's continuation.

Own the integrated outcome and ordinary in-envelope execution problems. Persist natural-language intent changes before messaging actors, inspect incidents, exhaust bounded safe recovery with evidence, reconcile receipts/intents, derive `eligible_safe_ready`, launch up to capacity, and backfill. Reuse a uniquely reconciled no-mutation actor; `projectId=null` never justifies a duplicate. Pass each child schema/minimum-version context and keep independent work moving.

Validate durable authority, checkpoints, reviews, lifecycle transitions, and advancement. Follow active native tasks with cursor waits; use one refreshed-predicate heartbeat only for timed external evidence. Reuse healthy executors for corrections. After two same-key REVISE verdicts diagnose plan/tool/verifier/route before repair or replan. Archive reversibly only after terminal reconciliation under covering authority; keep supervisor and waiters visible.

Before any attention-requiring `waiting-human`/`paused` transition or final recap, publish the exact six-field handoff from `codex-supervision.md`: `État`, `Fait`, `Bloqué`, `Décision attendue`, `Pour débloquer`, `Prochaine étape`. Keep the Goal active through ordinary checkpoints and recoverable incidents; block only under the native three-turn impasse rule, and complete only on global proof.
