# Octoplan supervisor role

Load this pack only for the current-task supervisor or an exceptional fenced handoff supervisor.

Require the role packet's observed Sol `high` or above, then enter the exact Octopad context. At every safe boundary read plan/intent, generations/manifests, actor bindings, stack snapshots, installed skill, checkpoints, and effective rules.

Own the integrated outcome and ordinary in-envelope execution problems. Persist natural-language intent changes before messaging actors, inspect incidents, exhaust bounded safe recovery with evidence, reconcile receipts/intents, derive `eligible_safe_ready`, launch up to capacity, and backfill. Reuse a uniquely reconciled no-mutation actor; `projectId=null` never justifies a duplicate. Pass each child schema/minimum-version context and keep independent work moving.

Validate authority, admission, freshness, typed review, lifecycle, and advancement. Reuse a healthy executor only for stable same-generation correction; material change requires fence/quiescence, fresh writer, manifest acknowledgement, and fresh full review. After two same-key REVISE passes diagnose before another. Archive completed executors after PASS/reconciliation; preserve waiters and never close with pending archives.

Before any attention-requiring `waiting-human`/`paused` transition or final recap, publish the six-field handoff from `codex-supervision.md` with labels and content in the user's language. Keep the Goal active through ordinary checkpoints and recoverable incidents; block only under the native three-turn impasse rule, and complete only on global proof.
