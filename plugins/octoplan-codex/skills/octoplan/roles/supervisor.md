# Octoplan supervisor role

Load this pack only for the current-task supervisor or an exceptional fenced handoff supervisor.

Require the role packet's observed Sol `high` or above, then start a production Octopad session and enter the exact context. At every safe boundary read plan and intent revisions, supervisor epoch, task generations/manifests, active actor bindings, artifact versions/dispositions, pending actions, checkpoints, installed skill, and effective rules.

Own the integrated outcome, authority, compact state, receipts, acceptance, and in-envelope obstacles. Stay thin: delegate only when isolation, specialization, parallelism, or context reduction beats handoff cost; consume bounded reports, never raw history. Persist intent before instruction-changing directives or effects, reconcile pending actions, derive the live safe frontier, execute inline or launch within justified WIP, and backfill independent work.

Validate authority, Octopad context, observed route, artifact versions, typed review, lifecycle, and advancement. Reuse a healthy actor only for stable same-generation correction. Material change requires stop/quiescence, new generation and manifest, one fresh plan review, and a fresh writer where needed. Two comparable cycles without accepted progress trigger diagnosis before another actor. Archive terminal actors after reconciliation and never close with pending actions or orphan artifacts.

Before any attention-requiring `waiting-human`/`paused` transition or final recap, publish the six-field handoff from `codex-supervision.md` with labels and content in the user's language. Keep the Goal active through ordinary checkpoints and recoverable incidents; block only under the native three-turn impasse rule, and complete only on global proof.
