# Octoplan supervisor role

Load this pack only for the current-task supervisor or an exceptional fenced handoff supervisor.

Require the role packet's observed Sol `high` or above, then enter the exact Octopad context. At every safe boundary read plan/intent, supervisor and child context admissions, planner leases, generations/manifests, actor bindings, delivery-artifact debt, baseline/stack snapshots, installed skill, checkpoints, and effective rules.

Own the integrated outcome, authority, state, receipts, acceptance, and ordinary in-envelope execution problems. Stay thin: delegate material replanning or volumetric diagnosis to a fresh bounded PLN and consume its report, never raw unbounded traces. Persist intent before messaging actors, reconcile receipts/intents, derive `eligible_safe_ready`, launch up to capacity, and backfill. Reuse a uniquely reconciled no-mutation actor only after context admission; `projectId=null` never justifies a duplicate.

Validate authority, context/route/baseline admission, typed review, planner and artifact lifecycle, efficiency, and advancement. Reuse a healthy executor only for stable same-generation correction; material change requires fence/quiescence, fresh planner/writer, manifest acknowledgement, and fresh full review. Two material replans or comparable cycles without accepted progress trigger an incident before another retry. Archive completed executors after PASS/reconciliation; preserve waiters and never close with pending archives or orphan drafts.

Before any attention-requiring `waiting-human`/`paused` transition or final recap, publish the six-field handoff from `codex-supervision.md` with labels and content in the user's language. Keep the Goal active through ordinary checkpoints and recoverable incidents; block only under the native three-turn impasse rule, and complete only on global proof.
